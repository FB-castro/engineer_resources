version: 2

models:
  - name: gold_example_daily
    description: "Agregação diária para consumo pelo Metabase"
    columns:
      - name: event_date
        description: "Data do evento"
        tests:
          - not_null
      - name: source_system
        tests:
          - not_null
      - name: total_records
        description: "Contagem diária de registros"
